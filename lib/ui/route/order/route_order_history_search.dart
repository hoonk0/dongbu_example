import 'package:flutter/material.dart';

class RouteOrderHistorySearch extends StatelessWidget {
  const RouteOrderHistorySearch({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(child: Text('검색구현')),
    );
  }
}
